model Sessions{
    id String @id @default(cuid())
    userId String
    jwtToken  String 
    device    String?
    ip        String?
    expiresAt DateTime
    user User @relation(fields: [userId],references: [id])

    @@map("sessions")
}